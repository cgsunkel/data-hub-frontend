import styled from 'styled-components'
import { FieldWrapper } from '../../../../components'

const FilterFieldWrapper = styled(FieldWrapper)`
  label {
    font-weight: normal;
  }
`

export default FilterFieldWrapper
